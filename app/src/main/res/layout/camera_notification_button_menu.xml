<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/toolbar_button_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_menu_white_36dp"
        android:background="@android:color/transparent"/>

    <FrameLayout
        android:id="@+id/notification_button_menu_layout"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_alignTop="@id/toolbar_button_menu"
        android:layout_alignEnd="@id/toolbar_button_menu"
        android:layout_alignRight="@id/toolbar_button_menu"
        android:background="@drawable/notification"
        android:visibility="invisible" />

</RelativeLayout>